package personio.company.employees.infrastructure;


import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import personio.company.employees.domain.Employee;
import personio.company.employees.domain.EmployeeId;
import personio.company.employees.domain.EmployeeName;


public class SQLiteEmployeeRepositoryTest {
    // @Test
    // void save_a_employee() {
    //     SQLiteEmployeeRepository repository = new SQLiteEmployeeRepository();
    //     Employee employee = new Employee(new EmployeeName("Paco"));

    //     repository.save(employee);
    // }

    // @Test
    // void return_an_existing_employee() {
    //     SQLiteEmployeeRepository repository = new SQLiteEmployeeRepository();
    //     Employee employee = new Employee(new EmployeeName("Nick"));
    //     EmployeeId employeeId = new EmployeeId(1);
    //     employee.setId(employeeId);
    //     employee.setSupervisorId(new EmployeeId(3));

    //     assertEquals(employee, repository.search(employee.id()));
    // }
}
